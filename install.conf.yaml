- defaults:
    link:
      relink: true

- create:
    - ~/.config/nvim

- clean: ['~','~/.config/nvim']

- link:
    ~/.zshrc: zshrc
    ~/.config/nvim/init.vim: init.vim
    ~/.p10k.zsh: p10k.zsh

- shell:
  - [git submodule update --init --recursive, Installing submodules]
